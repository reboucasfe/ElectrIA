import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.vibrantchameleonbounce',
  appName: 'vibrant-chameleon-bounce',
  webDir: 'dist'
};

export default config;
